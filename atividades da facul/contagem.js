let Inicio = parseInt(prompt('Me diga um número inteiro para iniciar a contagem: '))
let fim = parseInt(prompt('Me diga um número inteiro para terminar a contagem: '))
for (Inicio; fim ; fim--){
    alert(fim);
}